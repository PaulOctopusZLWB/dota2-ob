# M2 P1 pure-engine measurement

The normative run used one release-built foreground process on 2026-08-13:

```text
go build -trimpath -ldflags='-s -w' -o <run-dir>/m2-p1 ./cmd/m2-p1
<run-dir>/m2-p1 --samples docs/benchmarks/2026-08-13-m2-p1-samples.txt
<run-dir>/m2-p1 --summarize docs/benchmarks/2026-08-13-m2-p1-samples.txt
```

The fixed fixture is `m2-complete-ten-player.v3-fixed-state`, with ten visible
participants, sealed `HistoricalSnapshotManifestV1` and baseline contracts,
active-match exclusion, eligible-baseline membership in the immutable V2
lineage, the five M2 families, `config.v1`, rules `draft.v1`, `item.v1`,
`lane.v1`, `objective.v1`, and `readiness.v1`, and lineage identity
`2c79c5f26229fe7dbbaf3a42e86f827899387bdf8f763d22654c132493cf1f93`.
Every measured evaluation starts from the same empty bound policy state. The
causal evidence sequence and receive time are mutated for the sample, then the
canonical live-observation content hash is recomputed inside the timed update
before policy admission.

The process completed 10,000 untimed warmups followed by 100,000 measured
evaluations. Nearest-rank results were p50 2.854059 ms, p95 3.419509 ms, p99
3.719150 ms, and max 6.186351 ms. The independent raw-file summarizer reproduced
those exact values. It reported 30,946.7723 allocations and 1,880,506.92304
allocated bytes per evaluation. Baseline RSS was 4,947,968 bytes, post-warmup
RSS 10,833,920 bytes, peak RSS 12,472,320 bytes, final RSS 12,181,504 bytes, and
post-warmup growth 1,347,584 bytes. There were zero failures and zero excluded
samples.

Candidate SHA-256 was
`97a76d4ab8c57b7a274cd8af7fdb805ae150a78a424e85c686610f2e887309e1`, decision
SHA-256 was
`5106b43b2cbe9fb6ef02487eeee391d2c0701f89d0d6488636cf3871aae936be`, and
semantic state SHA-256 was
`268421f40bbc2f39b16532f4509b9bfcb265706fc403aadc7e0cb8c54c61f0d4`.

Host/runtime context: PaulPC4090, Linux kernel 7.0.0-28-generic amd64, Go 1.26.4
gc compiler, AMD Ryzen 9 7950X, 32 logical CPUs, NVIDIA 595.84 driver, CPU
performance governor, and recorded load average `6.05 7.23 8.04`.
Excluded stages were GSI capture and DotaTV delay, filesystem commit-log sync,
network, localization, rendering, delivery HTTP, and OBS.

All three bounds passed in the same run: p99 below 20 ms, peak RSS below 128
MiB, and post-warmup growth no more than 16 MiB.

Raw evidence is committed in `2026-08-13-m2-p1-samples.txt`; machine-readable
run context is in `2026-08-13-m2-p1-result.json`, and deterministic summary
output is in `2026-08-13-m2-p1-summary.txt`.
