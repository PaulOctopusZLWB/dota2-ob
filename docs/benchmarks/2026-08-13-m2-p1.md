# M2 P1 pure-engine measurement

The normative run used one release-built foreground process on 2026-08-13:

```text
go build -trimpath -ldflags='-s -w' -o <run-dir>/m2-p1 ./cmd/m2-p1
<run-dir>/m2-p1 --samples docs/benchmarks/2026-08-13-m2-p1-samples.txt
<run-dir>/m2-p1 --summarize docs/benchmarks/2026-08-13-m2-p1-samples.txt
```

The fixed fixture is `m2-complete-ten-player.v2`, with ten visible participants,
sealed `HistoricalSnapshotManifestV1` and baseline contracts, active-match
exclusion, and eligible-baseline membership in the immutable V2 lineage,
the five M2 families, `config.v1`, rules `draft.v1`, `item.v1`, `lane.v1`,
`objective.v1`, and `readiness.v1`, and lineage identity
`6f3c8eb1b59a9ab75c7e6b2f85f04bf516bfa57669c95810a86a5a0431ba181a`.

The process completed 10,000 untimed warmups followed by 100,000 measured
evaluations. Nearest-rank results were p50 3.658 ms, p95 4.392 ms, p99 4.952 ms,
and max 20.663 ms. The independent raw-file summarizer reproduced those exact
values. It reported 35,458.24 allocations and 2,563,367.02 allocated bytes per
evaluation. Baseline RSS was 5.41 MB, post-warmup RSS 10.75 MB, peak RSS 13.22
MB, final RSS 12.60 MB, and post-warmup growth 1.85 MB. There were zero failures
and zero excluded samples.

Candidate SHA-256 was
`97a76d4ab8c57b7a274cd8af7fdb805ae150a78a424e85c686610f2e887309e1` and
semantic state SHA-256 was
`bd0a145f4170c279d840e68456ca9b3d88b28e953698ae96f5b7288ba5085567`.

Host/runtime context: PaulPC4090, Linux kernel 7.0.0-28-generic amd64, Go 1.26.4
gc compiler, AMD Ryzen 9 7950X, 32 logical CPUs, NVIDIA 595.84 driver, CPU
performance governor, and recorded load average `8.71 8.76 9.09`.
Excluded stages were GSI capture and DotaTV delay, filesystem commit-log sync,
network, localization, rendering, delivery HTTP, and OBS.

All three bounds passed in the same run: p99 below 20 ms, peak RSS below 128
MiB, and post-warmup growth no more than 16 MiB.

Raw evidence is committed in `2026-08-13-m2-p1-samples.txt`; machine-readable
run context is in `2026-08-13-m2-p1-result.json`, and deterministic summary
output is in `2026-08-13-m2-p1-summary.txt`.
