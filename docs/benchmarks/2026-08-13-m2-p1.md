# M2 P1 pure-engine measurement

The normative run used one release-built foreground process on 2026-08-13:

```text
go build -trimpath -ldflags='-s -w' -o .artifacts/m2-p1 ./cmd/m2-p1
./.artifacts/m2-p1
```

The fixed fixture is `m2-complete-ten-player.v1`, with ten visible participants,
the five M2 families, `config.v1`, rules `draft.v1`, `item.v1`, `lane.v1`,
`objective.v1`, and `readiness.v1`, and lineage identity
`ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff`.

The process completed 10,000 untimed warmups followed by 100,000 measured
evaluations. Nearest-rank results were p50 2.871 ms, p95 3.528 ms, p99 3.926 ms,
and max 14.102 ms. It reported 31,461.68 allocations and 2,116,051.11 allocated
bytes per evaluation. Baseline RSS was 3.96 MiB, post-warmup RSS 11.38 MiB,
peak RSS 12.58 MiB, final RSS 12.44 MiB, and post-warmup growth 1.05 MiB.

Candidate SHA-256 was
`faf60392e657ca5b2226fe28d9004a4111f1a7d0674a32dcc084794a8e2e69fa` and
semantic state SHA-256 was
`d9bd30bc3cc89988b46b853836666d15a48ceeb937e5c648b470be542e305e63`.

Host/runtime context: Linux amd64, Go 1.26.4 gc compiler, 32 logical CPUs.
Excluded stages were GSI capture and DotaTV delay, filesystem commit-log sync,
network, localization, rendering, delivery HTTP, and OBS.

All three bounds passed in the same run: p99 below 20 ms, peak RSS below 128
MiB, and post-warmup growth no more than 16 MiB.
